public class SquareAndCube1
{
	public static void main(String[] args)
	{
		System.out.println("Number\tSquare\tCube");
		
		int number = 1;
		System.out.printf("%d\t%d\t%d%n",
			number, number * number, number * number * number);
		
		number = number + 1;
		System.out.printf("%d\t%d\t%d%n",
			number, number * number, number * number * number);
		
		number = number + 1;
		System.out.printf("%d\t%d\t%d%n",
			number, number * number, number * number * number);
		
		number = number + 1;
		System.out.printf("%d\t%d\t%d%n",
			number, number * number, number * number * number);
		
		number = number + 1;
		System.out.printf("%d\t%d\t%d%n",
			number, number * number, number * number * number);
		
		number = number + 1;
		System.out.printf("%d\t%d\t%d%n",
			number, number * number, number * number * number);
		
		number = number + 1;
		System.out.printf("%d\t%d\t%d%n",
			number, number * number, number * number * number);
		
		number = number + 1;
		System.out.printf("%d\t%d\t%d%n",
			number, number * number, number * number * number);
		
		number = number + 1;
		System.out.printf("%d\t%d\t%d%n",
			number, number * number, number * number * number);
		
		number = number + 1;
		System.out.printf("%d\t%d\t%d%n",
			number, number * number, number * number * number);
		
		number = number + 1;
		System.out.printf("%d\t%d\t%d%n",
			number, number * number, number * number * number);
		
	}
}
